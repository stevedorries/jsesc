{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Test",
            "type": "shell",
            "detail": "Runs the tests with the built-in Deno test runner",
            "group": {
                "kind": "test",
                "isDefault": true
            },
            "command": "deno test ./tests/tests.ts --allow-net",
            "problemMatcher": [
                "$tsc"
            ]
        }
    ]
}